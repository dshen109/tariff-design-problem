mcdp {
    provides consumed [kWh]
    constant intensity = 100 [g/kWh]

    requires offset [g]
    requires resulting [g]

    provided consumed * intensity <= required offset + required resulting
}
